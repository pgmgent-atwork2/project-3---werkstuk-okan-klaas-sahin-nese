<div class="input-container">
  <label for="{{name}}">{{label}}</label>
  <input class="input-login" type="{{type}}" name="{{name}}" value="{{value}}" />
  {{#if error}}
    <div class="error">{{error}}</div>
  {{/if}}
</div>